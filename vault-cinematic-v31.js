// Vault 3.1
(()=>{const css=document.createElement('link');css.rel='stylesheet';css.href='vault.css?v=3';css.dataset.vaultChamber='1';document.head.appendChild(css);const collection=document.querySelector('#collection');if(!collection)return;let on=localStorage.getItem('vaultSound')!=='off',audio;const btn=document.createElement('button');btn.className='vault-sound-toggle ghost-btn';btn.type='button';const label=()=>btn.textContent=on?'◉ Vault sound':'○ Vault sound';label();document.querySelector('.topbar-actions')?.prepend(btn);btn.onclick=()=>{on=!on;localStorage.setItem('vaultSound',on?'on':'off');label();if(on)cue(true)};function tone(type,f,a,d,v,b=f){const o=audio.createOscillator(),g=audio.createGain();o.type=type;o.frequency.setValueAtTime(f,a);o.frequency.exponentialRampToValueAtTime(b,a+d);g.gain.setValueAtTime(.0001,a);g.gain.exponentialRampToValueAtTime(v,a+.025);g.gain.exponentialRampToValueAtTime(.0001,a+d);o.connect(g).connect(audio.destination);o.start(a);o.stop(a+d+.02)}function cue(confirm=false){if(!on)return;try{audio||=new(window.AudioContext||window.webkitAudioContext)();audio.resume();const t=audio.currentTime;if(confirm){tone('sine',520,t,.18,.025,760);return}tone('square',78,t,.11,.035,48);tone('sine',42,t+.34,1.15,.042,31);tone('triangle',110,t+.46,.62,.022,62);tone('sine',440,t+1.02,.28,.018,660)}catch{}}const scene=document.createElement('div');scene.className='vault-cinematic';scene.setAttribute('aria-hidden','true');scene.innerHTML=`<div class="vault-light"></div><div class="vault-frame"><div class="vault-door vault-door-left"><i></i></div><div class="vault-door vault-door-right"><i></i></div><div class="vault-lock"><span>V</span><b></b></div><div class="vault-seam"></div></div><div class="vault-status">ACCESS GRANTED</div>`;document.body.append(scene);let timer;function play(){clearTimeout(timer);scene.classList.remove('is-opening');void scene.offsetWidth;cue();scene.classList.add('is-opening');timer=setTimeout(()=>scene.classList.remove('is-opening'),1900)}new MutationObserver(()=>{if(collection.classList.contains('active')&&!collection.dataset.vaultOpened){collection.dataset.vaultOpened='1';play()}else if(!collection.classList.contains('active'))delete collection.dataset.vaultOpened}).observe(collection,{attributes:true,attributeFilter:['class']})})();
