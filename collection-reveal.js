// A small, one-shot collection reveal without blocking navigation or interaction.
(()=>{const page=document.querySelector('#collection');if(!page)return;let timer,wasActive=page.classList.contains('active');new MutationObserver(()=>{const isActive=page.classList.contains('active');if(!isActive){wasActive=false;return}if(wasActive)return;wasActive=true;clearTimeout(timer);page.classList.remove('collection-arrive');void page.offsetWidth;page.classList.add('collection-arrive');timer=setTimeout(()=>page.classList.remove('collection-arrive'),1200)}).observe(page,{attributes:true,attributeFilter:['class']});})();
